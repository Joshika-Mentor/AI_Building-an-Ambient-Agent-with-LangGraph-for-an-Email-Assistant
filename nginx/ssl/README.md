# SSL Certificate Directory
Place your SSL certificate files here:
- `cert.pem` — SSL certificate (fullchain)
- `key.pem` — Private key

See `deploy/README.md` for instructions on generating certificates with Let's Encrypt.
